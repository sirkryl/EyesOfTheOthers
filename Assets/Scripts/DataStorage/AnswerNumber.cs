using UnityEngine;
using System.Collections;

public class AnswerNumber : MonoBehaviour {

	//what a ridiculous class..at the moment i need it for Dialog though because adding onClick-Listeners with ints are buggy
	public int number;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
